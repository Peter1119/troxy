# 세션 핸드오프 — 2026-04-22

> v0.3 머지 대기 + v0.4 Task #25 부분 완료 상태. 다음 세션에서 이어받을 수 있도록 맥락 정리.

## 한 줄 요약

**v0.3 TUI 재설계(Round 2~7) 완성 + 로컬 브랜치 `feature/v0.3-tui-redesign`에 머지 커밋 준비됨 → 사용자가 집에서 push + PR 생성 예정.** v0.4 Task #25는 probe R coverage gap 3건 pin 완료(afd13e1), 3건 남음.

## Git 브랜치 현황

```
main                        → 5367785  (origin/main, v0.2.0 태그)
feature/v0.2-adoption-push  → 410030b  (Round 2~7 우리 작업 전체, push 안 됨)
feature/v0.3-tui-redesign   → 4c5ef94  (main 머지 + cli/tui_app.py 삭제, push 대기)
feature/v0.4-refactor       → afd13e1  (v0.3 위에 Task #25 partial, push 대기)
```

**사용자 다음 액션**: 집에서 `feature/v0.3-tui-redesign` push + PR → 머지 후 `feature/v0.4-refactor` 도 push + PR.

### Push 절차 (사용자 직접)
```bash
cd ~/Workspace/troxy
# v0.3 먼저
git push -u origin feature/v0.3-tui-redesign
gh pr create --title "feat(v0.3): TUI 재설계 — tui/ 모듈 구조 + mock UX + filter/sort/pause" \
  --body-file docs/superpowers/handoffs/v0.3-pr-body.md  # (없으면 직접 작성)

# v0.3 머지 완료 후 v0.4
git push -u origin feature/v0.4-refactor
```

Sandbox 환경에서 SSH keychain 접근이 막혀서 claude 세션에서는 push 불가능. gh 계정 스위치는 `gh auth switch --user Peter1119`로 했지만 SSH key는 여전히 `peterhong-example`로 잡힘.

## v0.3 전체 범위 (Round 2~7)

15개 Bug fix + Hotfix 1건, 60 커밋, 242 tests 추가.

| Bug # | 내용 | Commit |
|-------|------|--------|
| #1 q 키 종료 | ef726d5 |
| #2 레이아웃 bottom bar | ef726d5 + 1f4de8d |
| #3 IP 잘못 표시 | dd0efc1 + afd7567 |
| #4 리스트 디자인 | ef726d5 + 7c81975 |
| #5 Enter detail 안 됨 | ef726d5 |
| #6 Ctrl+C 종료 | afd7567 |
| #7 Detail Esc 안 됨 | bd87ac1 |
| #8 Detail 디자인 | b7bbc70 + 525097a |
| #9 Detail 탭뷰 | 39cb5ac |
| #10 Mock body pretty JSON | 6d6ce0e |
| #11 Filter 4-field form | 32e69b3 |
| #12 Response scroll | 7ef5fda |
| #13 Bottom bar 3줄 | 198006a |
| #14 Filter inline | 4251ef0 (R5) → 410030b (R7 hidden-toggle 재설계) |
| #15 pause/resume (p 키) | c3e3ec1 + de38cc2 |
| #16 Mock URL 편집 | 9224611 |
| #17 Mock body Ctrl+L | 9224611 |
| #18 sort 토글 (s 키) | b5f279b |
| #19 InlineFilter hidden-toggle | 410030b |
| #20~#22 polish | 410030b |
| Hotfix | port 충돌 감지 | 8e8129d |

## Task #25 v0.4 Refactor — 진행 상황

**목표**: list_screen.py view extraction + probe R 패턴 전반 audit.

### ✅ 완료 (afd13e1)
- `test_mock_dialog_empty_host_falls_back_to_flow_value` — host fallback pin
- `test_mock_dialog_empty_path_falls_back_to_flow_value` — path fallback pin
- `test_x_clear_also_clears_filter_status_bar` — filter-status leak pin

Mutation probe 2/2 예측=실측 확인. 346 passed.

### 🔄 남은 작업 (우선순위 순)

1. **Task #25.1 list_screen view extraction** (가장 큰 작업)
   - 현재 `list_screen.py` 292줄 (b5f279b 이후 299 → 다시 trim으로 290 근처)
   - 경계 brittle 함 (code-review 지적)
   - `_poll_new_flows` + `_refresh_table_with_filter` table-rebuild 로직을 `list_helpers.table_rebuild()` 추출
   - 또는 `list_table.py` 신규 모듈로 view-rendering 책임 분리
   - 회귀 방지: 기존 테스트 100% PASS 유지 + table rebuild 경로 단위 테스트

2. **Task #25.4 Guard/Sync grep audit**
   - grep 패턴:
     - `or\s+f\[` — defensive fallback (mock_dialog 외 다른 데 있는지)
     - `add_class.*active` — UI sync 지점
     - `remove_class.*active` — UI hide 지점
     - `lockstep`, `in sync` — 주석 키워드
   - 각 match마다 "해당 방어 코드 제거 → 테스트 FAIL?" 확인
   - 없으면 해당 mutation 테스트 추가

3. **Dead code 제거** (Task #25.4 안에 포함 가능)
   - `mock_dialog.action_save`의 `method = ... if method_select.value else f["method"]` else 브랜치
   - Select(allow_blank=False)라 runtime 도달 불가
   - 제거하면 복잡도 감소

4. **(선택) 설계 Why 주석 복원** (Task #25.5)
   - Round 7 docstring trim 시 사용자 발언 quote 일부 제거됨
   - git blame으로 역추적 가능하나 extracted helper 파일에 복원 가치
   - Nice-to-have

## Memory 기록 (영구)

이 세션에서 memory에 저장된 규칙들 — 새 세션 자동 상속:

- `feedback_git_staged_check.md` — `git add X && git commit` 전 staged 상태 확인
- `feedback_team_default_model.md` — Agent spawn 시 `model: "sonnet"` 명시 필수
- `feedback_review_probe_patterns.md` — mutation probe 3종 (call-zero / 상수 unpin / liveness decay)
- `feedback_reviewer_practice.md` — **6 rules** (독립 probe / --run-slow gate / multi-agent git state / empty diff 체크 / 예측=실측 / probe R 휴리스틱)
- `feedback_user_quote_routing.md` — 사용자 인용 verbatim + 수신 경로 명시

## Team 상태

**troxy-v0.3-strict 팀 5명 아직 살아있음**:
- team-lead (나, opus)
- fixer (opus)
- design-review (opus)
- code-review (opus)
- qa-strict (opus)

**다음 세션 권장**: 현재 팀 해산 (`TeamDelete`) → v0.4 sonnet 팀 새로 스폰 (3명 정도로 슬림하게 — fixer, code-review, qa 정도로 충분).

팀 멤버 간 협업 패턴 memory에 기록됨. 새 sonnet 팀에 브리핑 시:
- "엄격 리뷰 6 rules 따를 것" (code-review)
- "커밋 메시지 predicted=실측 정확히 기록" (전원)
- "사용자 의도 인용은 verbatim + 수신 경로 명시"

## 주요 파일 맵

### v0.3 구조 (main merge 대기 중)
```
src/troxy/tui/
├── app.py                  # TroxyStartApp (textual.App)
├── proxy.py                # ProxyManager + ProxyBootError
├── list_screen.py          # 메인 리스트 (InlineFilter + DataTable + bottom bar)
├── detail_screen.py        # Request/Response 탭뷰
├── mock_dialog.py          # Mock CRUD (Host/Path/Method Input + Ctrl+L)
├── mock_list.py            # Mock 목록 (Space 토글 / d 삭제)
├── inline_filter.py        # 4-column 필터 위젯 (hidden-toggle)
├── widgets.py              # ConfirmDialog, Toast, CopyModal
├── detail_helpers.py       # url_summary, headers_block, body_renderable
├── list_helpers.py         # add_flow_row, update_cursor_marker, time_header_label
├── styles.py               # LIST_SCREEN_CSS, DETAIL_SCREEN_CSS, BOTTOM_BAR_CSS
├── theme.py                # STATUS_COLORS, METHOD_COLORS
├── copy.py                 # 모든 user-facing 텍스트 + 상수 (CA_TRUST_URL 등)
└── network.py              # get_local_ip (private RFC1918 우선)

tests/tui/*.py              # 242 tests (unit + integration + pexpect)
tests/tui/test_real_tty.py  # pexpect 기반 slow-marked TTY 테스트
```

### 삭제 대상 (feature/v0.3-tui-redesign에서 이미 삭제됨)
- `src/troxy/cli/tui_app.py` (800줄 단일 파일 — main의 구버전 TUI)

## 알려진 이슈 / 드리프트

1. **SVG 5건 drift** (git status M): `docs/qa-screenshots/list_*.svg + mock_dialog.svg` — 이전 세션의 재캡처 hash drift. 기능 무관, discard or 다음 커밋에 포함.
2. **회사 맥 dotfile** — `notify-stop.sh` 없던 문제는 `test -x && run || true` 가드로 해결 (claude-dotfiles 438129a). 사용자 회사 맥에서 `git pull` 필요.
3. **ssh-agent** 현재 `peterhong-example` 키만 로드. Peter1119 키 별도 추가 필요 시 사용자 직접.

## 다음 세션 시작 체크리스트

1. 이 핸드오프 문서 읽기 (`docs/superpowers/handoffs/2026-04-22-v0.3-ready-v0.4-start.md`)
2. `git log --oneline -5` — 현재 HEAD 확인
3. `git status` — dirty 파일 정리 판단
4. `uv run pytest tests/tui tests/unit` — 346 passed 확인
5. 사용자에게 v0.3 PR 머지 여부 확인 → YES면 Task #25.1부터, NO면 대기
6. sonnet 팀 스폰 여부 결정 (v0.4 범위가 작으면 direct work도 OK)

## Round 2~7 에이전트 기여 (참고)

| Role | 주요 성취 |
|------|----------|
| fixer | Bug #1/#5/#6/#7/#15/#18 구현, Real-TTY ground truth 도입 |
| design-review | Detail/Filter/MockDialog 재설계, 터미널 폰트 fallback 대응 (🎭→🧩) |
| code-review | 6 rules 수립, 리뷰어 독립 probe R 정착, coverage gap 2건 발견 |
| qa-strict | SVG/pexpect/3-gate 인프라, layout/mock 회귀 가드 |

**이 협업 체계와 규칙이 memory에 영구 저장됨. v0.4 팀에 자동 상속.**
