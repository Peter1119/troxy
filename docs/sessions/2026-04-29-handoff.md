# Session Handoff — 2026-04-29

## Branch: main

## 오늘 한 일 (Summary)
- mock body 입력/수정 UX가 어렵다는 사용자 피드백을 출발점으로, MockDialog의 페인포인트를 코드 리딩으로 정리 (`src/troxy/tui/mock_dialog.py`).
- brainstorming 스킬을 통해 "외부 에디터 연동(Ctrl+E)으로 body를 풀스크린 편집" 방향을 합의.
- 폴백 체인, JSON 검증 시점, 임시 파일 처리까지 설계 합의 완료. 코드는 아직 손대지 않음.
- 후속 단계는 spec 문서 작성 → writing-plans → 구현. 이번 핸드오프 시점에는 설계 합의만 git에 박는다.

## 의사결정 로그 (Decisions)
| 결정 | 이유 | 검토한 대안 |
|------|------|-------------|
| 1차 페인포인트는 **body 보기/수정 편의성**으로 한정 | AI가 페이로드를 넣더라도 사람이 직접 보고 고치기 쉬워야 함. headers 편집/JSON 검증은 별개 작업으로 빼기로 함 | (B) headers 편집 UI 부재, (C) JSON 검증 부재, (D) 셋 다 동시 — 모두 보류 |
| 편집 위치는 **`$EDITOR` 외부 에디터** (A안) | 풀스크린 + 익숙한 키바인딩으로 보기/수정 둘 다 해결. 내부 BodyEditScreen 자체 구현보다 가벼움 | (B) 내부 풀스크린 Screen, (C) 둘 다, (D) modal 키우기 |
| 트리거는 **MockDialog 내부에서 Ctrl+E** (A안) | 짧은 body는 modal에서 빠르게, 길면 외부 에디터로 빠지는 흐름 | (B) modal 열리자마자 자동 실행 — 짧은 body 수정에 과함, (C) detail에서 분기 — UX 분기 복잡 |
| 폴백 체인 **`$VISUAL → $EDITOR → nano → vi → toast 실패`** | macOS/Linux 기본 설치된 nano를 우선 (vim 학습 부담 회피, 사용자 본인이 vim 어려워함). 사용자가 명시한 환경변수가 있으면 존중. vi는 최후 보루 | vim 우선 — 사용자 본인이 어렵다고 함, 제거. strict 모드(env 미설정 시 즉시 에러) — 폴백이 더 친절 |
| JSON 검증은 **저장(Ctrl+S) 시점에만, content_type이 JSON 계열일 때만** (A안) | 편집 중 잔소리 안 함. JSON 외 body(plain text/XML)는 자유롭게. 실패 시 Toast로 라인 위치 표시 + 저장 차단 | (B) 종료 직후 즉시 검증, (C) 검증 안 함, (D) 강제 우회 단축키 — YAGNI |
| 임시 파일은 **content_type 기반 확장자 + 종료 직후 즉시 삭제** (A+D) | 신택스 하이라이팅으로 가독성 ↑. 응답에 토큰 등 민감정보 가능성 → 디스크에 남기지 않음 | 항상 .json/.txt — 하이라이팅 손해, OS temp 방치 — 보안 리스크 |

## 현재 진행 상태 (Progress)
- [x] 페인포인트 진단 (`MockDialog`의 8줄 TextArea 한계, headers 편집 부재, JSON 검증 부재)
- [x] 설계 합의 (Ctrl+E → `$EDITOR` 폴백 → 임시파일 → 검증 → modal 복귀)
- [ ] spec 문서 작성 (`docs/superpowers/specs/2026-04-29-mock-body-editor-design.md`)
- [ ] writing-plans 스킬로 구현 계획 작성
- [ ] 구현: MockDialog에 Ctrl+E 바인딩 추가, `App.suspend()`로 Textual 일시정지, 임시파일 IO, JSON 검증 with line/col, 폴백 체인
- [ ] 테스트: `$EDITOR` 미설정/설정/존재하지 않는 바이너리/저장 안 하고 종료 등 케이스
- [ ] QA 스크린샷 갱신

## 다음에 할 일 (Next Steps)
1. 다음 세션에서 brainstorming 마무리 — design 섹션(아키텍처/컴포넌트/에러처리/테스트) 정리하고 spec 파일로 떨어뜨림.
2. spec 사용자 리뷰 후 `writing-plans` 스킬 호출 → implementation plan 작성.
3. 구현 진입 전 `using-git-worktrees`로 워크트리 생성 (`.worktrees/mock-body-editor`).
4. 구현 시 `App.suspend()` 컨텍스트 매니저로 Textual 일시정지하고 외부 에디터 호출 — 백그라운드 프로세스 금지 (터미널 충돌).
5. 폴백 체인은 `shutil.which()`로 PATH에서 바이너리 탐색 — 설치 여부 확인 후 호출.

## 주의사항 / 알려진 이슈 (Caveats)
- 현재 워킹트리에 QA 스크린샷 SVG 9개가 modified 상태인데, 이번 세션 작업과 **무관**한 자동 갱신물로 추정. 핸드오프 커밋에는 포함하지 않음. 다음 세션에서 의도된 변경인지 확인 필요.
- `MockDialog.action_save`의 headers 처리는 현재 원본 통과만 함 (`_serialize_headers`가 dict/str/None 분기만). headers 편집 UI는 **이번 작업 범위 밖** — 별도 작업으로 분리.
- `add_mock` 흐름의 빈 modal에서 시작하는 케이스도 동일한 페인포인트(좁은 TextArea)를 가지므로 Ctrl+E 동작 시 동일하게 작동해야 함.
- Windows 지원은 사실상 비대상 (mitmproxy 의존 + Textual `App.suspend()` 동작 차이) — 명시적 제외 가능.

## 코드 변경 맥락 (Change Context)
- 이번 세션은 **코드 변경 없음**. 핸드오프 문서 1개만 추가.
- 영향 받을 파일은 다음 세션에서:
  - `src/troxy/tui/mock_dialog.py` — Ctrl+E 바인딩, 외부 에디터 호출, JSON 검증, 임시파일 처리.
  - 신규 `src/troxy/tui/external_editor.py` (예상) — `$EDITOR` 탐색/실행/폴백 로직 분리. mock_dialog.py가 이미 291줄이라 책임 분리 권장.
  - 테스트 추가 위치 미정 — 외부 프로세스 호출이라 단위 테스트는 monkeypatch로 `subprocess.run` mock.
